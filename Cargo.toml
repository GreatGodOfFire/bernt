[workspace]
resolver = "2"
members = ["bernt-position", "bernt-movegen", "bernt-test", "bernt-search", "bernt", "bernt-tuning", "bernt-datagen"]

[profile.release]
panic = 'abort'
lto = true
codegen-units = 1
strip = true
